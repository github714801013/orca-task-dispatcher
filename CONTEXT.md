# Orca 任务分发器

将待开发任务经人工确认后分配到目标仓库，并启动独立开发会话的技能上下文。

## 领域语言

**任务**：
来自任务源、具有稳定标识且可被分发的一项开发工作。
_避免_：工单、Issue、Jira 单

**候选资格**：
任务通过当前流程 Jira 条件筛选的证明；查询解析成功、父子字段后置过滤完成后逐任务记录。流程节点声明 `requires_candidate_eligibility: true` 时，它是 `decide` 与 `launch` 的共同硬门槛，不能由显式仓库、分支、快照或人工推测替代；节点未声明该要求时两个命令都跳过校验。proposal 还要求任务或父任务参考方案非空；direct 同样要求资格证明，但不要求参考方案。
_避免_：候选仓库、仓库映射、兜底路由

**仓库映射兜底**：
仅在候选资格已通过、但参考方案无法唯一映射仓库/租户/分支时，使用 GitNexus、标题、描述等证据解决映射不确定性；不得反向赋予任务候选资格。
_避免_：候选资格兜底、扩大 JQL

**候选仓库**：
位于项目根目录内、已验证可作为任务开发目标的 Git 仓库。
_避免_：任意路径、工作树

**任务工作区**：
任务专属的 Git worktree 检出目录，名为 `<任务编号>-<语义slug>`（无 slug 时即 `<任务编号>`），其中任务编号必须是上游完成父产品需求归一化后的 `task_id`，由 Orca 创建在 `<Orca workspaceDir>/<仓库名>/` 下；原始开发子任务编号使用 `source_task_id` 保留，仅用于 direct/proposal 上下文，不用于工作区、状态或制品身份。`parent_task_id` 必须省略或等于 `task_id`，仅作父任务上下文，Dispatcher 不根据它自动替换 `task_id`；提供 `parent_task_id` 时必须同时保留 `source_task_id`。创建后把分支改名为 `<用户名>/<工作区名>`，再由 dev-spec-gen 同步未托管文件并修补 IDE 配置。可以由无状态的 `worktree create` CLI 预先创建，也可由 launch 按同一稳定身份创建/复用；任务制品和开发会话仍归 launch 管理。
_避免_：工作树路径（未验证）、临时目录

**任务分配**：
用户为一个任务确认的候选仓库与可选开发基线的组合。
_避免_：自动路由、默认仓库

**运行计划**：
由一组任务分配确定性生成的、按任务分配展开的分发安排；每个任务分配在其任务工作区里开启一个独立开发会话；流程不要求独立工作树时会话落在候选仓库 checkout。
_避免_：独立 checkout、Agent 编排、开发计划、tab 分组

**开发会话**：
任务工作区里由 `orca terminal create --command claude` 显式开启、接收一次任务文本的独立 Claude 会话；分发不为它注入监督协议，也不等待它结束。同一工作区可存在多个流程会话；只有 state 记录的首次投递摘要与本次文本一致时才判定为重复。
_避免_：受监督 worker、协调者、调度者、仅按活动终端一律阻断

**投递回执**：
`orca terminal send --wait-submit` 返回的接收证据，分两级：`accepted` 证明输入被接受，`turn_started` 证明观察到本轮执行；只有 `accepted` 时仍记为已分发，但保留「启动未确认」告警。
_避免_：发送成功、已执行

**已分发任务**：
对应流程的开发会话已接受任务文本（回执 `accepted`）的任务；它是该流程状态长期去重的唯一依据。物理工作区中不同流程的状态仍独立，首次投递摘要相同才共享一次会话。
_避免_：已完成任务、已启动任务、已执行任务

**未确定分发**：
分发结果无法确认、必须由人工核对并复位后才能再次尝试的任务状态，例如会话未就绪、投递被拒绝、主机不支持投递观察或发送后状态落盘失败。
_避免_：可自动重试失败
