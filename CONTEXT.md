# Orca 任务分发器

将待开发任务经人工确认后分配到目标仓库，并启动独立开发会话的技能上下文。

## 领域语言

**任务**：
来自任务源、具有稳定标识且可被分发的一项开发工作。
_避免_：工单、Issue、Jira 单

**候选仓库**：
位于项目根目录内、已验证可作为任务开发目标的 Git 仓库。
_避免_：任意路径、工作树

**任务工作区**：
任务专属的 Git worktree 检出目录，名为 `<任务编号>-<语义slug>`（无 slug 时即 `<任务编号>`），由 Orca 创建在 `<Orca workspaceDir>/<仓库名>/` 下；创建后把分支改名为 `<用户名>/<工作区名>`，再由 dev-spec-gen 同步未托管文件并修补 IDE 配置。分发 worker 直接绑定于此。
_避免_：工作树路径（未验证）、临时目录

**任务分配**：
用户为一个任务确认的候选仓库与可选开发基线的组合。
_避免_：自动路由、默认仓库

**运行计划**：
由一组任务分配确定性生成的、按任务分配展开的分发安排；每个任务分配生成一个独立受监督 worker，落在该任务的任务工作区；流程不要求独立工作树时落在候选仓库 checkout。
_避免_：独立 checkout、Agent 编排、开发计划、tab 分组

**已分发任务**：
已成功为目标任务工作区启动受监督 worker 的任务；它是长期去重的唯一依据。
_避免_：已完成任务、已启动任务

**未确定分发**：
受监督 worker 的启动结果无法确认、必须由人工复位后才能再次尝试的任务状态。
_避免_：可自动重试失败
