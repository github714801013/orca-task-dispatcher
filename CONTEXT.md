# Orca 任务分发器

将待开发任务经人工确认后分配到目标仓库，并启动独立开发会话的技能上下文。

## 领域语言

**任务**：
来自任务源、具有稳定标识且可被分发的一项开发工作。
_避免_：工单、Issue、Jira 单

**候选仓库**：
位于项目根目录内、已验证可作为任务开发目标的 Git 仓库。
_避免_：任意路径、工作树

**任务分配**：
用户为一个任务确认的候选仓库与可选开发基线的组合。
_避免_：自动路由、默认仓库

**运行计划**：
由一组任务分配确定性生成的、按仓库分组并在现有 checkout 的一个 Orca Tab 内最多容纳四个 pane 的分发安排。
_避免_：独立 checkout、Agent 编排、开发计划

**已分发任务**：
已成功将开发请求发送给目标开发会话的任务；它是长期去重的唯一依据。
_避免_：已完成任务、已启动任务

**未确定分发**：
开发请求的发送结果无法确认、必须由人工复位后才能再次尝试的任务状态。
_避免_：可自动重试失败
